{% extends "@Contao/frontend_module/_base.html.twig" %}

{% block content %}

    {% if isError is defined and isError %}
        {{ errorMessage|raw }}
    {% endif %}

    {% if showTokenForm is defined and isError is defined and showTokenForm and not isError %}
        {{ tokenForm|raw }}
    {% endif %}

    {% if isError is defined and not isError %}
        {{ successMessage|raw }}
    {% endif %}

{% endblock %}
